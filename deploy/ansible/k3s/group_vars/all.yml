# 全局变量

# Tailscale 配置
tailscale_authkey: "{{ lookup('env', 'TAILSCALE_AUTHKEY') }}"

# 监控栈配置
grafana_admin_password: "admin123"

# Next Terminal 配置
next_terminal_db_password: "next-terminal"

# 资源限制 (适配 4GB 内存)
resources:
  prometheus:
    memory_limit: "256Mi"
    memory_request: "128Mi"
  loki:
    memory_limit: "256Mi"
    memory_request: "128Mi"
  grafana:
    memory_limit: "256Mi"
    memory_request: "128Mi"
  tempo:
    memory_limit: "256Mi"
    memory_request: "128Mi"
  next_terminal:
    memory_limit: "256Mi"
    memory_request: "128Mi"
